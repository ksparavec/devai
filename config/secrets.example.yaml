# Secrets template - NEVER commit actual secrets
# Copy to secrets.yaml and fill in values (secrets.yaml is gitignored)

api_keys:
  openai: ""
  anthropic: ""
  google: ""

# For cloud deployments, these are stored in:
# - AWS: Secrets Manager or SSM Parameter Store
# - Azure: Key Vault
# - GCP: Secret Manager
