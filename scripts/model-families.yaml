# Model families tracked by DevAI.
#
# This file is the ONLY hand-maintained source of model information. All
# sizes, architectures, and per-variant metadata are fetched live from
# upstream providers (HuggingFace Hub API, Ollama registry) by
# scripts/generate-catalog.py, which writes deploy/models.yaml.
#
# Each family may specify:
#   ollama_repos    – list of library names under ollama.com/library/<name>;
#                     every published tag for each library becomes a catalog
#                     row served by the ollama backend
#   hf_repos        – list of HuggingFace repositories (transformers
#                     safetensors / FP8 / NVFP4 / AWQ / GPTQ); each becomes
#                     one catalog row served by vllm/sglang backends
#   gguf_repos      – list of HuggingFace GGUF-only repositories. Each
#                     entry can carry an optional `include:` allowlist of
#                     quantization tokens (substring-matched against the
#                     filename, case-insensitive) and an optional
#                     `tag_prefix:` that anchors the local Ollama tag.
#                     Example: filename Qwen3.5-27B-UD-Q3_K_XL.gguf with
#                     tag_prefix=27b becomes ollama tag
#                     qwen3.5:27b-ud-q3_k_xl. Wired by the gguf source
#                     kind (commit 2 of the simplification refactor).
#   arch_ref        – HF repo whose config.json holds the architecture
#                     for variants that don't publish a full config
#                     (primarily Ollama GGUF tags; quantization does not
#                     change architecture, so the base model's config is
#                     the authoritative spec for every tag in the family)
#   thinking        – PRE-PROBE HINT only. Final reasoning capability is
#                     determined per model variant at runtime by
#                     scripts/probe-ollama-reasoning.py and stored in
#                     the probe cache as `reasoning.capability`.
#                     This flag is informational; the picker and router
#                     ignore it and use the probe result instead.
#   parsers         – CURATED HINT for vLLM/SGLang only. Names the backend
#                     server-startup parser flags this family expects to
#                     work with. The probe drivers launch with these flags
#                     set and verify them in a single round-trip; the
#                     router reads the probe-confirmed values back from the
#                     cache. Shape:
#                       parsers:
#                         vllm:
#                           reasoning: <vllm --reasoning-parser value>
#                           tool: <vllm --tool-call-parser value>
#                         sglang:
#                           reasoning: <sglang --reasoning-parser value>
#                           tool: <sglang --tool-call-parser value>
#                     All four sub-fields are optional. A family without a
#                     `parsers:` block probes in inline/no-tool mode (no
#                     parser flags emitted; same behavior as today). Only
#                     applies to hf_repos / NVFP4 / safetensors variants —
#                     Ollama handles reasoning and tool-calling natively.
#   defaults        – advisory default tags per backend (optional)
#   discover        – OPTIONAL override for `make catalog-discover` (the
#                     read-only newer-version finder, scripts/catalog-
#                     discover.py). Discovery is HYBRID: by default each
#                     family's lineage (brand + version + sub-lineage) and
#                     its trusted authors are AUTO-DERIVED from the family
#                     `name` and its existing hf_repos / ollama_repos /
#                     gguf_repos -- no `discover:` block needed (qwen3.5 ->
#                     auto-finds qwen3.6/qwen3.7, llama3.2 -> llama3.3,
#                     etc). Add a `discover:` block ONLY to override that:
#                       discover:
#                         enabled: false        # skip this lineage entirely
#                         hf_authors:           # REPLACES the auto-derived
#                           - nvidia            # author allowlist (use to
#                           - apolo13x          # RESTRICT to vetted authors)
#                         name_regex: 'Qwen3'   # REPLACES the default brand
#                                               # match (widen/narrow; also
#                                               # disables the structural
#                                               # line-membership filter)
#                         min_version: '3.5'    # quote it -- bare 3.10 is a
#                                               # YAML float (== 3.1)
#                         ollama_names:         # extra ollama libraries to
#                           - qwen-next         # probe by exact name
#                     Every sub-field is optional; a present field replaces
#                     its auto-derived counterpart (ollama_names is
#                     additive). The block is read ONLY by catalog-discover;
#                     generate-catalog.py ignores it. catalog-discover never
#                     edits anything -- it prints candidates to review.
#
# YAML formatting rule: every list element on its own line. Do NOT use
# inline brackets `[a, b]` or inline maps `{key: val}`.
#
# To add a family: add a block below, then run `make catalog-regen`.
#
# ── SGLang NVFP4 status (updated 2026-07-09) ─────────────────────────────
# Image `lmsysorg/sglang:v0.5.10.post1-cu130` DOES serve NVFP4 now. The
# earlier "every NVFP4 cell fails at modelopt_quant.py:1482 in fp4_quantize"
# was a symptom of the piecewise CUDA-graph default: it torch.compiles the
# forward, and Dynamo cannot trace flashinfer's FP4 JIT-compile path (a
# subprocess/threading.Lock call), so warmup crashes. The router + prober
# now always pass `--disable-piecewise-cuda-graph`, which runs FP4 eagerly.
# With it, 8 models serve on SGLang (Qwen3-8B/14B, Llama-3.1-8B,
# Nemotron-Nano-9B, gpt-oss-20b, DeepSeek distills, Qwen3-14B-FP8).
# Genuine arch/quant gaps still fail on SGLang (Gemma-4 family, Qwen3.5/3.6
# MoE, diffusiongemma, Qwen3-Coder-30B, Nemotron-3-Nano-30B) -- those serve
# on vLLM, and the probe cache marks them error/unsupported so the picker
# hides them. Drop the flag when a future SGLang image traces the FP4 path.

families:
  - name: gemma4
    ollama_repos:
      - gemma4
    hf_repos:
      - google/gemma-4-e2b-it
      - google/gemma-4-e4b-it
      - google/gemma-4-26b-a4b-it
      - google/gemma-4-31b-it
      # Two NVFP4 quants of the 26B-A4B MoE -- the NVIDIA-official one
      # ships with the gemma4 MTP drafter wired in; the bg-digitalservices
      # variant predates the drafter and stays MTP-less for comparison.
      - repo: nvidia/Gemma-4-26B-A4B-NVFP4
        mtp:
          method: mtp
          drafter: google/gemma-4-26B-A4B-it-assistant
          num_speculative_tokens: 4
      - bg-digitalservices/Gemma-4-26B-A4B-it-NVFP4
      - repo: nvidia/Gemma-4-31B-IT-NVFP4
        mtp:
          method: mtp
          drafter: google/gemma-4-31B-it-assistant
          num_speculative_tokens: 4
      - google/gemma-4-12B-it
      - bg-digitalservices/Gemma-4-E4B-it-NVFP4
      - unsloth/gemma-4-12b-it
    gguf_repos:
      # Registry's gemma4:31b at Q4_K_M needs ~46G — doesn't fit on a
      # 24G card. Unsloth's Dynamic 2.0 Q3 variants drop weights to
      # ~14-17G so the 31B becomes reachable on consumer hardware.
      - repo: unsloth/gemma-4-31B-it-GGUF
        tag_prefix: 31b
        include:
          - UD-Q3_K_XL
          - UD-IQ3_XXS
      - repo: unsloth/gemma-4-26B-A4B-it-GGUF
        tag_prefix: 26b-a4b
        include:
          - UD-Q3_K_XL
      - repo: google/gemma-4-26B-A4B-it-qat-q4_0-gguf
        tag_prefix: 26b-a4b
        include: []   # TODO: quant allowlist e.g. UD-Q3_K_XL ([] emits nothing)
    parsers:
      # Gemma-4 emits the `gemma4` tool-call format -- verified on the
      # sibling diffusiongemma family (same 26B-A4B MoE backbone, probe
      # recorded tool_parser=gemma4 verified=true). Without this block the
      # probe never attempts a tool parser, so the router strips
      # tools/tool_choice and the model scored 0 on the tools bench despite
      # tool-calling fine (its Ollama GGUF twin scored tools=1.00). vLLM
      # only: Gemma-4 NVFP4 does not serve on SGLang (arch/quant gap).
      # Reasoning deliberately omitted -- the -it instruct variants probe as
      # capability=none (non-reasoning); revisit if a thinking variant lands.
      vllm:
        tool: gemma4
    arch_ref: google/gemma-4-26b-a4b-it
    thinking: true        # hint; runtime probe shows e2b/e4b are unsupported

  - name: diffusiongemma
    # DiffusionGemma 26B-A4B-it: Google's discrete-diffusion dLLM on the
    # Gemma 4 26B/A4B MoE backbone (arch DiffusionGemmaForBlockDiffusion,
    # block-autoregressive multi-canvas sampling). vLLM-only: SGLang has
    # no diffusion path and cannot serve NVFP4 (see the SGLang NVFP4 note
    # above), and Ollama/llama.cpp has no block-diffusion sampler. Needs
    # the vLLM gemma-cu130 image (>=0.22.1). 128K context ceiling on the
    # 24G card; the load-critical memory tuning (--max-num-batched-tokens
    # 512 et al.) lives in deploy/recovery-flags.json. Multimodal in
    # (text+image+video), tuned text-first on 24G.
    hf_repos:
      - nvidia/diffusiongemma-26B-A4B-it-NVFP4
    parsers:
      vllm:
        reasoning: gemma4
        tool: gemma4
    arch_ref: nvidia/diffusiongemma-26B-A4B-it-NVFP4
    thinking: true

  - name: qwen3.6
    ollama_repos:
      - qwen3.6
    gguf_repos:
      - repo: unsloth/Qwen3.6-35B-A3B-GGUF
        tag_prefix: 35b-a3b
        include: []   # TODO: quant allowlist e.g. UD-Q3_K_XL ([] emits nothing)
      - repo: unsloth/Qwen3.6-35B-A3B-MTP-GGUF
        tag_prefix: 35b-a3b
        include: []   # TODO: quant allowlist e.g. UD-Q3_K_XL ([] emits nothing)
      - repo: unsloth/Qwen3.6-27B-MTP-GGUF
        tag_prefix: 27b
        include: []   # TODO: quant allowlist e.g. UD-Q3_K_XL ([] emits nothing)
      - repo: unsloth/Qwen3.6-27B-GGUF
        tag_prefix: 27b
        include: []   # TODO: quant allowlist e.g. UD-Q3_K_XL ([] emits nothing)
    # Qwen3.6 family. The text-only NVFP4 variant from
    # sakamakismile is the only catalog row today and is built
    # around the built-in MTP head (mtp_num_hidden_layers=1 in
    # text_config, applied recursively for K speculative tokens).
    # vLLM 0.20+ handles this via the `qwen3_5_mtp` method --
    # different code path from the broken gemma4_mtp.py weight
    # loader, so this is the live MTP target on the 24G card.
    # No Ollama repos / GGUF repos here -- stock Ollama and
    # llama.cpp ignore the MTP head; PR #22673 is the
    # llama.cpp pre-merge branch that knows what to do with it.
    hf_repos:
      - repo: sakamakismile/Qwen3.6-27B-Text-NVFP4-MTP
        mtp:
          method: qwen3_5_mtp
          num_speculative_tokens: 3
      - nvidia/Qwen3.6-35B-A3B-NVFP4
      - unsloth/Qwen3.6-27B-NVFP4
      # NO mtp: block for the pi-tune below, on purpose. Benched 2026-07-02 on
      # the 24G card: the qwen3_5_mtp draft lm_head (~2.37 GiB, vocab-sized so
      # context-INDEPENDENT) OOMs at MODEL LOAD -- serve @32K died ~0.5 GiB
      # short (needed 2.37, 1.86 free) in qwen3_5_mtp.py ParallelLMHead init,
      # BEFORE KV allocation. So it is NOT recoverable by lowering
      # --gpu-memory-utilization, --enforce-eager, or a smaller context (all
      # act after the failing allocation). Baseline (MTP off) serves fine @32K
      # ~33 tok/s. Do not re-add an mtp: block here without ~0.5 GiB more
      # usable VRAM or a draft-head-tied vLLM MTP build. The fit probe DID
      # catch this: a separate -mtp pass records mtp_fits=false / oom_startup.
      # The gap is that NOTHING consumes mtp_fits -- the router serves ::mtp off
      # the catalog mtp block alone -- so a stray mtp block here would still be
      # advertised and OOM at serve. (The Text-NVFP4-MTP sibling above is
      # text-only with more headroom; its mtp block is untested here.)
      - sakamakismile/Qwen3.6-27B-MTP-pi-tune-NVFP4
    parsers:
      vllm:
        reasoning: qwen3
        tool: qwen3_xml
    arch_ref: sakamakismile/Qwen3.6-27B-Text-NVFP4-MTP
    thinking: true

  - name: qwen3.5
    ollama_repos:
      - qwen3.5
    hf_repos:
      # Qwen3.5-9B-NVFP4 -- the 9B size ships with the same
      # mtp_num_hidden_layers=1 head as the 27B (verified in the
      # upstream config.json) and ykarout's NVFP4 quant preserves
      # all 15 MTP tensors in the safetensors index. Fits the 24G
      # ceiling because the model is only ~5-6 GB at NVFP4 and the
      # qwen3_5_mtp.py drafter scaffold (~2.4 GB in vLLM 0.20.x)
      # has plenty of room.
      - repo: ykarout/Qwen3.5-9B-NVFP4
        mtp:
          method: qwen3_5_mtp
          num_speculative_tokens: 3
      - apolo13x/Qwen3.5-27B-NVFP4
      - apolo13x/Qwen3.5-35B-A3B-NVFP4
    parsers:
      vllm:
        reasoning: qwen3
        # Qwen3.5 chat template emits XML-shape tool calls
        # (`<tool_call><function=...><parameter=key>value</parameter></function></tool_call>`),
        # not plain hermes (`<tool_call>{json}</tool_call>`). Probe
        # confirmed `hermes` left tool_calls empty on Qwen3.5-9B-NVFP4
        # while it worked on Qwen3-8B-NVFP4. Verified via
        # tokenizer_config.json chat_template inspection 2026-05-01.
        tool: qwen3_xml
      sglang:
        reasoning: qwen3
        # SGLang has no qwen3_xml parser — `qwen` covers the same
        # XML-shape via the Qwen25 detector. Exercised on SGLang now that
        # --disable-piecewise-cuda-graph unblocks NVFP4 (Qwen3-8B/14B serve
        # with R=qwen3 T=qwen); see the SGLang NVFP4 status block above.
        tool: qwen
    gguf_repos:
      # 27B dense — Q4_K_M from registry spills hard at 24G; sub-Q4
      # Unsloth quants fit. UD-Q3_K_XL is the recommended sweet spot;
      # Q3_K_M / UD-IQ3_XXS give 1-2 GB more headroom for KV.
      - repo: unsloth/Qwen3.5-27B-GGUF
        tag_prefix: 27b
        include:
          - UD-Q3_K_XL
          - UD-IQ3_XXS
          - Q3_K_M
      # 35B-A3B MoE — not in the Ollama registry. Sub-Q4 quants put
      # this in reach on a 24G card (only ~3B active params per token,
      # so it loads like a 3B for compute but with 35B knowledge).
      - repo: unsloth/Qwen3.5-35B-A3B-GGUF
        tag_prefix: 35b-a3b
        include:
          - UD-Q3_K_XL
          - UD-IQ3_XXS
          - Q3_K_M
    arch_ref: ykarout/Qwen3.5-9B-NVFP4
    thinking: true

  - name: qwen3
    # Same model on both backends so the picker can show side-by-side
    # rows for benchmarking. The tag filter in generate-catalog.py
    # drops bare aliases (`:8b`, `:14b`) and routing variants
    # (`:30b-instruct`, `:235b-thinking`) so only quantization-bearing
    # tags reach the catalog.
    ollama_repos:
      - qwen3
    hf_repos:
      # Dense — pick one (or more) that fits your VRAM band. NVFP4 weights:
      #   8B  → ~6 GB    (fast iteration, fits everywhere)
      #   14B → ~8 GB    (sweet spot on 24G)
      #   32B → ~19 GB   (largest dense that fits 24G with KV)
      - nvidia/Qwen3-8B-NVFP4
      - nvidia/Qwen3-14B-NVFP4
      - nvidia/Qwen3-32B-NVFP4
      # MoE — 30B total, 3B active per token. Loads like a 30B (~17 GB
      # NVFP4) but compute-light. Distinct from the qwen3.5 family's
      # 35B-A3B variants which have a different tokenizer.
      - nvidia/Qwen3-30B-A3B-NVFP4
      - nvidia/Qwen3-14B-FP8
    parsers:
      vllm:
        reasoning: qwen3
        tool: hermes
      sglang:
        reasoning: qwen3
        tool: qwen
    arch_ref: nvidia/Qwen3-14B-NVFP4
    thinking: true

  - name: qwen3-coder
    # Agentic-coding fine-tune of Qwen3-30B-A3B (30.5B total / 3.3B
    # active MoE). Native 256K context, extends to 1M with YaRN.
    # Non-thinking only — model card explicitly states it does not
    # generate <think></think> blocks, so no reasoning parser is set.
    # Tool calls follow the Qwen3-Coder format which both vLLM and
    # SGLang register as `qwen3_coder` (same name on both, unlike the
    # nemotron split). On-disk weights ~18 GB; same VRAM envelope as
    # the base nvidia/Qwen3-30B-A3B-NVFP4 above.
    hf_repos:
      - NVFP4/Qwen3-Coder-30B-A3B-Instruct-FP4
    parsers:
      vllm:
        tool: qwen3_coder
      sglang:
        tool: qwen3_coder
    arch_ref: NVFP4/Qwen3-Coder-30B-A3B-Instruct-FP4
    thinking: false

  # NOTE: NVFP4/Qwen3-30B-A3B-Thinking-2507-FP4 was added 2026-05-02
  # and removed the same day. The checkpoint OOMs at vLLM model-load
  # time on 24G consumer GPUs — verified on RTX 4000 Pro Blackwell
  # with the full mitigation matrix (--enforce-eager,
  # --kv-cache-dtype fp8, --max-num-seqs 4, --gpu-memory-utilization
  # 0.95). PyTorch reaches 22.74 GiB allocated and fails on a 768 MiB
  # final tensor before reaching the memory-profiling stage where any
  # of those flags would have effect, so no engine_flags knob can
  # rescue it. Root cause: this checkpoint's config.json carries
  # intermediate_size=6144 vs Coder/base Qwen3-30B-A3B's 5472, which
  # inflates vLLM's load-time scratch buffers by ~700 MiB beyond the
  # 24G ceiling. Re-add when:
  #   * vLLM loader stops sizing scratch buffers to intermediate_size
  #     for MoE-only models (issue worth filing upstream), OR
  #   * NVIDIA / RedHatAI publishes a Thinking-2507 NVFP4 quant with
  #     a slimmer load-time profile (e.g. the Coder/Instruct line),
  #     OR
  #   * SGLang fixes its NVFP4 path (bug noted at the top of this
  #     file) so we can fall back to SGLang's loader.

  - name: ornith
    # Ornith-1.0 (deepreinforce-ai) -- an agentic-coding LLM family, RL
    # self-improving, MIT licensed. On a 24G card the 9B fits as GGUF (Ollama)
    # or as an in-house NVFP4 safetensors quant on vLLM (see MEASURED below);
    # the 35B / 397B are out of scope for 24G.
    #
    # The 9B is DENSE qwen3_5 arch (Qwen3_5ForConditionalGeneration; 32L,
    # 16 attn / 4 KV heads, head_dim 256, vocab 248320, native 256K ctx,
    # bf16), post-trained on Gemma 4 + Qwen 3.5. Opens with a <think> block;
    # tool calls are qwen3_xml. The model card prescribes --reasoning-parser
    # qwen3 --tool-call-parser qwen3_xml (sglang: qwen) -- recorded here for
    # when a 24G-fitting NVFP4/FP8 quant is published; Ollama parses natively,
    # so no `parsers:` block is needed for the GGUF rows.
    #
    # MEASURED on the 24G card (RTX PRO 4000 Blackwell), 2026-07-14:
    #   9B GGUF (Ollama): Q4_K_M loads + generates, <think> reasoning works
    #     (probe capability=structured), ~13.4 GB resident at load (5.2 GB
    #     weights). Q5_K_M / Q6_K / Q8_0 / bf16 are the same arch at larger
    #     sizes and fit too. This is the ONLY 9B path that fits 24G.
    #   9B bf16 safetensors (vLLM): OOMs at model load (oom_startup) at EVERY
    #     tier incl. 32K -- NOT catalogued. The fit-math estimate (~17.5 GiB
    #     weights + FP8 KV -> fits @32K) was optimistic: the vocab-248320
    #     embed + lm_head, the MTP nextn layer vLLM loads, and CUDA workspace
    #     push the load past 24 GiB (GPU confirmed clear).
    #   9B NVFP4 safetensors (vLLM): the fix. Quantized IN-HOUSE from the bf16
    #     source via `make quant-local` (scripts/quant/quant_smoke.py preset
    #     ornith) -> ksparavec/Ornith-1.0-9B-NVFP4 (hf_repos below). Text linears
    #     W4A4 NVFP4; lm_head / embeddings / MoE gates / linear-attn AND the
    #     whole vision tower kept bf16 -- so vision stays a working feature and
    #     the multimodal Qwen3_5ForConditionalGeneration wrapper is preserved
    #     (vLLM has no text-only Qwen3_5ForCausalLM loader; a text-only quant
    #     will not load). PROBED 2026-07-16 (probe-vllm) on the 24G card: the
    #     FULL native 256K context fits at 20.61 GB (FlashInfer NVFP4 GEMM),
    #     capability=structured (qwen3 <think> reasoning), tool parser qwen3_xml
    #     verified, ~122s cold start. The ONLY safetensors path onto 24G.
    #
    # No `mtp:` block: the qwen3_5_mtp draft lm_head is vocab-sized, and this
    # vocab (248320) is ~1.6x the usual, so it would OOM at load on 24G (same
    # class as the bf16 OOM above and the qwen3.6 family note).
    #
    # 35B (qwen3_5_moe, multimodal): the arch IS supported by vLLM + SGLang
    # (probe-verified via scripts/probe-ornith-arch.sh -- Qwen3_5MoeFor-
    # ConditionalGeneration is registered), but bf16 (~70 GB) and FP8
    # (~35 GB) exceed 24G outright, GGUF Q4_K_M (~20 GiB) needs llama.cpp
    # qwen3_5_moe support (absent), and no NVFP4 quant is published. NOT
    # catalogued; a ~19-20 GiB NVFP4 quant is the only route onto a 24G card.
    gguf_repos:
      - repo: deepreinforce-ai/Ornith-1.0-9B-GGUF
        tag_prefix: 9b
        include:
          - Q4_K_M
          - Q5_K_M
          - Q6_K
          - Q8_0
          - bf16
    hf_repos:
      # In-house NVFP4 quant of the 9B (see the 9B NVFP4 MEASURED note above).
      - ksparavec/Ornith-1.0-9B-NVFP4
    parsers:
      # Model card prescribes --reasoning-parser qwen3 --tool-call-parser
      # qwen3_xml. SGLang loadability of the multimodal qwen3_5 wrapper is
      # UNVERIFIED (only vLLM is proven); the sglang parser names stay inert
      # until an SGLang probe produces a fit cell.
      vllm:
        reasoning: qwen3
        tool: qwen3_xml
      sglang:
        reasoning: qwen3
        tool: qwen
    arch_ref: deepreinforce-ai/Ornith-1.0-9B
    thinking: true

  - name: llama3.2
    # Ollama-only — NVIDIA hasn't published an NVFP4 quant for the
    # Llama-3.2 1B/3B line (too small for the Blackwell tensor-core
    # target). No HF sibling to add for cross-backend comparison.
    ollama_repos:
      - llama3.2
    arch_ref: nvidia/Llama-3.1-8B-Instruct-NVFP4
    thinking: false

  - name: llama3.1
    # Both backends. Ollama's llama3.1 library covers 8B/70B/405B in
    # every k-quant; the HF NVFP4 sibling is the 8B Instruct so picker
    # rows align on size class for side-by-side benchmarking.
    ollama_repos:
      - llama3.1
    hf_repos:
      - nvidia/Llama-3.1-8B-Instruct-NVFP4
    parsers:
      # Llama-3.1 Instruct has no structured-reasoning surface — only
      # tool-calling is curated here. Reasoning probe will fall back to
      # inline/unsupported classification.
      vllm:
        tool: llama3_json
      sglang:
        tool: llama3
    arch_ref: nvidia/Llama-3.1-8B-Instruct-NVFP4
    thinking: false

  - name: nemotron-nano-v2
    hf_repos:
      - nvidia/NVIDIA-Nemotron-Nano-9B-v2-NVFP4
    parsers:
      vllm:
        reasoning: nemotron_v3
        tool: nemotron_json
      sglang:
        reasoning: nemotron_3
        # No tool entry on purpose -- see the tool-parser note below.
    # SGLang had NO parsers block until 2026-07-26, and the cost was
    # measured: with no curated hint the prober recorded
    # reasoning_parser=null and capability=inline, so the <think> trace
    # stayed in `content` and HumanEval scored **0.04 on SGLang against
    # 0.80 on vLLM** for the same checkpoint at the same ctx, while GSM8K
    # was 0.98 on both (its extractor finds a final number, which appears
    # in the reasoning trace anyway). The bench correctly flagged a drop;
    # the drop was an artefact of the missing parser, not of the model.
    # That is the same ~0.10 HumanEval floor described below for vLLM,
    # from the same cause.
    #
    # `nemotron_3` is registered in the pinned SGLang v0.5.10.post1 image
    # (verified by introspecting ReasoningParser.DetectorMap) and its
    # detector is built on "<think>"/"</think>" -- the exact tokens this
    # model's chat template emits (verified in tokenizer_config.json). No
    # per-model image override is needed on the SGLang side.
    # UNVERIFIED end to end: this needs `make probe-sglang` to confirm the
    # round-trip and a re-bench to confirm the HumanEval recovery.
    #
    # Reasoning parser nemotron_v3 is NOT registered in the pinned vLLM
    # v0.22.1 image (passing it crashes the engine at startup, container
    # exits before /health). It IS registered in vLLM v0.25.1, to which
    # this model is pinned via the per-model image override in
    # deploy/recovery-flags.json. That override also supplies the two
    # flags the hybrid Mamba-Transformer checkpoint requires:
    # --mamba-ssm-cache-dtype float32 and --trust-remote-code. Verified by
    # direct podman launch on v0.25.1: <think> traces separate cleanly
    # into the reasoning field, content carries clean executable code (no
    # </think> leak). This is what lifts HumanEval off its ~0.10 floor.
    # NOTE: v0.25.1 loops on greedy (temp 0) reasoning for this model --
    # serve/bench at temp>=0.6 (NVIDIA's recommended Nemotron sampling),
    # else the reasoning trace runs away and no answer is emitted.
    #
    # Tool parser: the 9B-v2 chat template uses NVIDIA's custom uppercase
    # `<TOOLCALL>[...]</TOOLCALL>` format, which no stock vLLM parser
    # matches. We wire NVIDIA's own nemotron_json tool parser as a plugin
    # (scripts/vllm_plugins/nemotron_json_tool_parser.py, registered in
    # deploy/vllm-plugins.json) -- its import paths are ported to the
    # v0.25.1 module layout this model runs on; the logic is upstream's.
    # The prober/router emit --tool-parser-plugin <file> +
    # --tool-call-parser nemotron_json. vLLM only (SGLang has no plugin
    # analogue). If the probe fails to verify tool round-trips, the router
    # falls back to stripping tools -- plain chat + reasoning still work.
    arch_ref: nvidia/NVIDIA-Nemotron-Nano-9B-v2-NVFP4
    thinking: true        # hint; vLLM/SGLang variants probed separately later

  # Parser-name verification source: vLLM v0.x (cu130 image) registers
  # reasoning parsers in vllm/reasoning/__init__.py:_REASONING_PARSERS_TO_REGISTER
  # and tool-call parsers in --help under --tool-call-parser choices.
  # SGLang v0.5.10 registers reasoning detectors in
  # python/sglang/srt/parser/reasoning_parser.py and tool detectors in
  # python/sglang/srt/function_call/function_call_parser.py.
  # Drift here breaks container startup — keep names verbatim per backend.

  - name: nemotron-3-nano
    ollama_repos:
      - nemotron-3-nano
    hf_repos:
      # nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4 — text-only sibling
      # of the Omni variant below.
      #
      # History:
      #   2026-05-01: removed after a probe at 24 GB returned
      #     gpu_model_runner.py:4818 OOM during model load (PyTorch
      #     reaches 23.37 GiB on a 23.42 GiB-usable card and fails on a
      #     316 MiB allocation, with 4.25 GiB reserved-but-unallocated).
      #   2026-05-02: tested the original --enforce-eager hypothesis;
      #     FALSIFIED because the OOM trips before CUDA graph capture.
      #   2026-05-02: vLLM's own error message hinted at the fix —
      #     PYTORCH_ALLOC_CONF=expandable_segments:True. Combined with
      #     --enforce-eager + --gpu-memory-utilization 0.92, the model
      #     loads cleanly: VRAM peaks at 23.12/23.89 GiB (96.8 % of card,
      #     ~1.3 GiB headroom at 32K) and round-trips both the reasoning
      #     parser (nemotron_v3) and tool parser (qwen3_coder, NOT hermes
      #     which JSON-decoder-errored on the model's <function=...>
      #     <parameter=...> tool format).
      # Re-added 2026-05-03 with the recovery flags wired into the
      # router's per-model overrides — see modelRecoveryConfig in
      # gpu-arbiter/main.go. The flag list lives there rather than in a
      # catalog field because (a) it's recovery policy not probe-verified
      # data, (b) it's the same on every (vram, ctx) cell, and (c) the
      # probe-cache schema doesn't carry flag-list fields.
      - nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-NVFP4
      #
      # nvidia/Nemotron-3-Nano-Omni-30B-A3B-Reasoning-NVFP4 (and the
      # matching unsloth GGUF mirror) was previously listed here.
      # Removed 2026-05-03: project focus is coding + reasoning, the
      # Omni variant only adds vision/audio I/O on top of the same
      # reasoning brain, and on a 24G card it would consume both
      # additional disk and the headroom that the text-only variant
      # already sits within. The Ollama side never wired multimodal
      # tokens through `/api/chat` either, so the GGUF mirror was
      # text-only inference of the Omni weights — branding without
      # delivered capability. Re-add only when the project explicitly
      # adds image/audio workflows.
    parsers:
      # Reasoning parser: SGLang ships `nemotron_3`; vLLM has a built-in
      # `nemotron_v3` that the project initially used, but NVIDIA's HF
      # model card for the 30B-A3B variant explicitly prescribes a
      # *different* parser called `nano_v3`, registered via a plugin
      # script (`nano_v3_reasoning_parser.py`) shipped in the model
      # repo. The plugin inherits from `DeepSeekR1ReasoningParser` and
      # adds the `enable_thinking=False` swap for reasoning-disabled
      # turns. The built-in `nemotron_v3` is for the older 9B-v2 line
      # and gives different boundaries on this model -- empirically
      # observed 2026-05-03: under Anthropic-style chat templates
      # (Claude Code) the boundary detection failed and raw
      # `<function=...>` Qwen3-Coder tool tags leaked into `content`.
      # We now vendor `nano_v3_reasoning_parser.py` under
      # `scripts/vllm_plugins/` and resolve it via the plugin registry
      # (`deploy/vllm-plugins.json`); the router auto-emits
      # `--reasoning-parser-plugin <abs> --reasoning-parser nano_v3`.
      # Names DO NOT match across backends -- keep both verbatim.
      # Tool parser: the prior comment claimed `hermes`. That was wrong
      # -- verified empirically 2026-05-02 against the text-only
      # NVFP4 variant: the model emits Qwen3-Coder-style
      # `<function=NAME><parameter=KEY>VALUE</parameter></function>`
      # tags (NOT JSON-in-`<tool_call>` like Hermes), and the hermes
      # parser JSONDecodeError'd on every call. `qwen3_coder` parsed
      # the round-trip cleanly into OpenAI-format tool_calls.
      vllm:
        reasoning: nano_v3
        tool: qwen3_coder
      sglang:
        reasoning: nemotron_3
        tool: qwen3_coder
    arch_ref: nvidia/NVIDIA-Nemotron-Nano-9B-v2-NVFP4
    thinking: true        # hint upgraded after probe verified structured

  - name: nemotron-cascade-2
    # Ollama-only — the only public HF variant (nvidia/Nemotron-Cascade-
    # 2-30B-A3B) is bf16 safetensors at ~60 GB and OOMs on 24G VRAM.
    # Probed 2026-05-01 across vLLM + SGLang × {16G,24G} × {32K..256K};
    # every cell returned oom_startup. NVIDIA hasn't published an
    # NVFP4 / FP8 / quantized variant of Cascade-2. Re-add hf_repos
    # only when a quantized release lands.
    ollama_repos:
      - nemotron-cascade-2
    arch_ref: nvidia/NVIDIA-Nemotron-Nano-9B-v2-NVFP4
    thinking: true        # hint upgraded after probe verified structured

  - name: nemotron
    # Ollama-only — the legacy `nemotron` library hosts the 70B
    # Llama-3-Nemotron variant in k-quants only. NVIDIA's NVFP4 line
    # for that generation never shipped publicly.
    ollama_repos:
      - nemotron
    arch_ref: nvidia/NVIDIA-Nemotron-Nano-9B-v2-NVFP4
    thinking: false       # generic Nemotron, no reasoning per model card

  - name: deepseek-r1-distill
    # Both backends. Ollama's `deepseek-r1` library publishes the
    # full distill matrix (1.5B/7B/8B/14B/32B/70B Qwen + Llama). HF
    # mirrors are the BF16 originals — no NVFP4 distill exists.
    # Tag scheme on Ollama side: `<size>-<base>-distill-<quant>`,
    # e.g., `deepseek-r1:14b-qwen-distill-q4_K_M`.
    ollama_repos:
      - deepseek-r1
    hf_repos:
      # Official DeepSeek R1 distills — BF16 safetensors. NVIDIA hasn't
      # published NVFP4 variants of the distills, and the community
      # quants are unverified. BF16 fits on 24G:
      #   Qwen-7B  → ~14 GB (Qwen2 arch)
      #   Llama-8B → ~15 GB (Llama-3 arch)
      # Both emit R1-style `<think>` blocks; the deepseek-r1 reasoning
      # parser strips them into `reasoning_content`.
      #
      # Tool calling goes through the `deepseek_string` plugin parser
      # (scripts/vllm_plugins/deepseek_string_tool_parser.py, registered
      # in deploy/vllm-plugins.json). The R1-Distill chat templates emit
      # the DeepSeek-V3 boundary markers `<｜tool▁call▁begin｜>` etc.
      # literally, but the distills inherit the BASE model's tokenizer
      # (Qwen2 / Llama3) which lacks those markers as atomic vocab
      # entries. vLLM's built-in `deepseek_v3` / `_v31` / `_v32` parsers
      # all do `vocab.get(<token>)` at startup and crash with HTTP 500:
      # "DeepSeek-V3 Tool parser could not locate tool call start/end
      # tokens in the tokenizer". The `deepseek_string` plugin
      # re-implements the same parser with substring matching instead
      # of vocab-id lookups, so it works on any tokenizer that emits
      # the markers as text — atomic-token or BPE-split.
      # SGLang side stays empty: SGLang's plugin model is Python-import-
      # based (not file-path), and a separate plugin would be needed.
      # Until that lands, SGLang traffic for this family runs without
      # tool support.
      - deepseek-ai/DeepSeek-R1-Distill-Qwen-7B
      - deepseek-ai/DeepSeek-R1-Distill-Llama-8B
    parsers:
      vllm:
        reasoning: deepseek_r1
        tool: deepseek_string
      sglang:
        reasoning: deepseek-r1
    arch_ref: deepseek-ai/DeepSeek-R1-Distill-Qwen-7B
    thinking: true

  - name: gpt-oss
    # OpenAI's first open-weight release. The 20B variant ships natively
    # in MXFP4 (~13 GB on disk) — vLLM and SGLang both load it directly.
    # Uses the harmony chat format with explicit reasoning channels.
    hf_repos:
      - openai/gpt-oss-20b
    parsers:
      # Reasoning parser handles the `<|channel|>analysis<|message|>...`
      # harmony reasoning blocks. vLLM registers it under
      # `openai_gptoss`; SGLang registers it under `gpt-oss`.
      # Tool parser: vLLM has dedicated `openai` (handles harmony tool
      # channels); SGLang exposes `gpt-oss` for the same purpose.
      vllm:
        reasoning: openai_gptoss
        tool: openai
      sglang:
        reasoning: gpt-oss
        tool: gpt-oss
    arch_ref: openai/gpt-oss-20b
    thinking: true
