apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devai-work
  namespace: devai
  labels:
    app: devai-lab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # storageClassName is set by cloud-specific overlays
