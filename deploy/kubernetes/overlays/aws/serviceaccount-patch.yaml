apiVersion: v1
kind: ServiceAccount
metadata:
  name: devai
  annotations:
    # IRSA - IAM Roles for Service Accounts
    # Replace with actual role ARN from Terraform
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/devai-lab-eks-role
