apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: devai

resources:
  - ../../base
  - storageclass.yaml

commonLabels:
  cloud: gcp

patches:
  - path: ingress-patch.yaml
  - path: serviceaccount-patch.yaml
