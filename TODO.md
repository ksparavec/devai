# TODO

## Local AI Agent Integration
- [ ] Install a local AI coding agent (e.g. Continue, Aider, or similar) in JupyterLab/code-server that uses the router API (devai-router:11434) for inference
- [ ] Configure Claude Code, Codex, and Gemini CLIs to optionally use local models via the router as a backend/fallback

## Open Items
- [ ] README.md — full rewrite of remaining legacy sections (appendices, detailed GPU docs)
- [ ] Test suite — update test-router.sh for model switch and API translation edge cases
- [ ] devai.sh / ollama.sh — rewrite standalone scripts for production use
